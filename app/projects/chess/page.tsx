import React, { FunctionComponent } from 'react';

export type ChessPageProps = {
  // No Props
};

const ChessPage: FunctionComponent<ChessPageProps> = (props) => {
  return <div className=''>Chess</div>;
};

export default ChessPage;
