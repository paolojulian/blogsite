import React, { FunctionComponent } from 'react';

export type ProjectCardProps = {
    // No Props
};

const ProjectCard: FunctionComponent<ProjectCardProps> = (props) => {
  return (
    <div className="h-[350px] w-[475px] bg-white"></div>
  );
}

export default ProjectCard;
